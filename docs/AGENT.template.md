# `{{DIR_NAME}}/` Agent Guide

补充目录级 runbook。Repo 级规则先看 `{{ROOT_CLAUDE_PATH}}`，这里不重复。

> 带 `TODO` 的模板文件不算已落地规范，也不应被当作 canonical source。
> 首次引入后，应尽快补全真实入口、命令、测试和边界。

## 你在这里主要处理什么

- TODO: 说明 `{{DIR_NAME}}/` 的核心职责
- TODO: 说明 `{{DIR_NAME}}/` 的关键入口或模块
- TODO: 说明 `{{DIR_NAME}}/` 的交付边界

## 进入前先知道

- TODO: 写一条在 `{{DIR_NAME}}/` 最容易踩坑的局部规则
- TODO: 写一条在 `{{DIR_NAME}}/` 最关键的流程约束
- TODO: 写一条在 `{{DIR_NAME}}/` 最关键的联动提醒

## 常用命令

```bash
# TODO: 补一条在 {{DIR_NAME}}/ 最常用的命令
# TODO: 再补一条在 {{DIR_NAME}}/ 最常用的命令
```

## 改动后优先检查

- TODO: 列出改动后需要优先跑的测试文件（如 `tests/test_xxx.py`）
- TODO: 列出改动后需要同步检查的联动文件

## 常见联动

- TODO: 写一条改动本目录后，极易被遗漏的外部联动点（如 "改了 DB schema 记得更新 frontend types"）
