# ADR 索引（指针页）

> 本仓库已存在小写 ADR 目录 [`docs/adr/`](../adr/)，为避免与 `docs/ADR/` 在大小写
> 不敏感文件系统（macOS APFS / Windows NTFS）上撞车，**ADR 正文继续放小写目录**，
> 本页只做导航与状态索引（AI 协作标准要求 `docs/ADR/README.md` 存在）。
> 长期建议：择机把 `docs/adr/` 整体并入本目录（`git mv` 保留历史），与本页合并。

## ADR 一览

| ADR | 标题 | 状态 | 日期 |
| --- | ---- | ---- | ---- |
<!-- 新增 ADR 时在此添加一行：| [NNN](../adr/NNN-slug.md) | 标题 | 状态 | 日期 | -->

## 使用约定

- 新建 ADR：放入 `../adr/`，命名 `NNN-slug.md`，并在本页补一行索引。
- 本页不复制正文内容；决策细节打开对应 ADR 文件阅读。