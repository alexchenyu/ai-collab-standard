{
  "version": 1,
  "_comment": "Generated by .ai-collab/scripts/init_ai_collab_docs.sh --install-cursor-hook. Edit freely; init won't overwrite. Hook script lives in .ai-collab/scripts/cursor-doc-check-hook.sh and only runs governance checks when AI-collab docs are edited (AGENTS.md, CLAUDE.md, lesson_learned*, .cursor/rules/*.mdc, etc). See https://cursor.com/docs/hooks for the full event list.",
  "hooks": {
    "afterFileEdit": [
      {
        "command": "bash .ai-collab/scripts/cursor-doc-check-hook.sh"
      }
    ]
  }
}
