{
  "version": 1,
  "_comment": "Generated by .ai-collab/scripts/init_ai_collab_docs.sh --install-cursor-hook. Edit freely; init won't overwrite. afterFileEdit runs governance checks when AI-collab docs are edited (AGENTS.md, CLAUDE.md, lesson_learned*, .cursor/rules/*.mdc, etc); beforeShellExecution is a deterministic shell-discipline gate (denies bare python/pip per behavior rule R6; override patterns via <repo-root>/.ai-collab-shell-deny.txt). See https://cursor.com/docs/hooks for the full event list and docs/QUALITY_GATES.md for the gate inventory.",
  "hooks": {
    "afterFileEdit": [
      {
        "command": "bash .ai-collab/scripts/cursor-doc-check-hook.sh"
      }
    ],
    "beforeShellExecution": [
      {
        "command": "bash .ai-collab/scripts/cursor-shell-guard-hook.sh"
      }
    ]
  }
}
